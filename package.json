{
  "scripts": {
    "dev": "concurrently --prefix \"{name}\" --names \"RAILS,REACT\" -c \"bgBlue,black.bgGreen,black.bgYellow\" \"rails s -p 3001\" \"(cd client && npm run start)\"",
    "deploy": "rsync -avz -e 'ssh' --exclude-from '.rsync-exclude' . scylla:/home/scylla/books",
    "build": "(cd client && npm run build)"
  },
  "devDependencies": {
    "concurrently": "^3.4.0"
  }
}
